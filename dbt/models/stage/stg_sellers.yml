version: 2

models:
  - name: stg_sellers
    description: "Sellers table."
    columns:
      - name: seller_id
        description: Unique seller identifier per order.
        tests:
          - unique
          - not_null
      - name: seller_city
        description: Seller city.
      - name: seller_state
        description: Seller state (UF).